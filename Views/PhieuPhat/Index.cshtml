@model IEnumerable<THPTPhuBinh.Models.PhieuPhat>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LyDo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhieuMuon.MaNV)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LyDo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhieuMuon.MaNV)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MaPP }) |
                @Html.ActionLink("Details", "Details", new { id=item.MaPP }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MaPP })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
